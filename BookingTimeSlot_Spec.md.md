# 預約流程時間區段邏輯實作需求

請實作預約流程中的日期與時間可用性判斷

## 目標
讓使用者只能選到真正可預約的日期與時間，所有不可選項目都要反灰顯示並禁止操作

## 日期規則
- 休假日（holiday）不可選，以日為單位
- 不可選日期要保留在月曆中，但顯示為 disabled、不可選、無任何提示
- 時區處理：統一使用 `Asia/Taipei`
- 最小預約置前時間：尚不需要，但可以先實作起來

## 時間規則
- 時間區塊以 30 分鐘為單位
- 只顯示營業時間範圍內的可用時段
- 超出營業時間的區段不要顯示
- break time 不可選，並以 disabled 顯示
- 已被占用的時間不可選：
  - `CONFIRMED`
  - `PENDING` 且 `expiredAt > now`
- 時段 overlap 判斷需以資料庫中的實際預約資料為準

## 服務時長計算
- 需讀取資料庫中的服務時長
- 以 30 分鐘為基準計算占用區塊
- 判斷可用性時，若候選時段與既有預約區塊重疊，則不可選
- 計算方式：
  - `occupiedBlocks = ceil(duration / 30)`

## UI 要求
- 不可選日期與時間都要反灰

## 驗收標準
- holiday 不能選
- break time 不能選
- `PENDING` 與 `CONFIRMED` 都會阻擋同時段
- 非營業時間不顯示
- 不可選項目在 UI 上清楚呈現為 disabled 狀態
