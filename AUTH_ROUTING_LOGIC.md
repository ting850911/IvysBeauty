# 系統認證與轉導邏輯 (Auth Routing Logic)

這份文件用於統一管理並記錄系統中所有與「登入、註冊、權限防護」相關的轉導（Redirect）邏輯，作為未來開發與重構（例如使用 `proxy.ts`）的準則。

## 📍 頁面導向與防護規則

- **點選登入按鈕**：
  點選登入按鈕後，應該轉往登入頁面，而不是註冊頁面。

- **登入頁面初始狀態**：
  任何跟登入相關的頁面，初始畫面都應該是登入（Login）而不是註冊（Register）。

- **登入成功後的資料檢查**：
  登入成功應該確認會員資料是否完整，要有「手機」及「生日」。若無，則前往 `CompleteProfile` 填寫。

- **註冊成功後的動線**：
  註冊成功後，應該轉到「登入」畫面（而非直接放行）。

- **預約按鈕防護**：
  點選任何跟預約相關的按鈕時，應該要確認狀態是否有登入成為會員。如果沒有，則轉到登入；若有，才可以開始預約。

- **登出動線**：
  點選登出時，應該回到首頁 `/`。

- **受保護的頁面 (Protected Routes)**：
  `history`（預約紀錄）、`booking`（預約流程）等頁面，都需要是會員才可以瀏覽，否則一律轉到登入頁面。
